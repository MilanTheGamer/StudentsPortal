{
  "name": "students_portal_registration",
  "version": "1.0.0",
  "description": "a micro service for students to register",
  "main": "index.js",
  "scripts": {
    "start": "node ./bin/www.js",
    "dev": "nodemon -r dotenv/config ./bin/www.js dotenv_config_path=./server/config/development.env",
    "cluster": "nodemon -r dotenv/config ./bin/cluster.js dotenv_config_path=./server/config/development.env",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Milan",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^10.0.0",
    "express": "^4.17.1",
    "html-pdf": "^3.0.1",
    "mongoose": "^5.13.7",
    "multer": "^1.4.3",
    "nodemailer": "^6.6.3"
  },
  "devDependencies": {
    "nodemon": "^2.0.12"
  }
}
